export class Lexer {
    constructor(input) {
        this.input = input;
        this.position = 0;
    }

    /*
    Задача лексера — преобразование исходного текста C++ в токены. Это может включать:

    - Токенизация ключевых слов (int, return, if, и т.д.).
    - Обработка идентификаторов, операторов и разделителей.
    - Обработка ошибок для некорректного синтаксиса.
    */
    tokenize() {
        return [];
    }
}